{
  "name": "@scrapio/api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run build -w @scrapio/scraper && tsx watch src/server.ts",
    "worker": "npm run build -w @scrapio/scraper && tsx watch src/worker.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "@scrapio/scraper": "file:../../packages/scraper"
  }
}
